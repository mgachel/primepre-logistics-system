import { CustomerDailyUpdates } from '@/components/daily-updates';

/**
 * Customer Daily Updates Page
 * Shows Air/Sea tabs with Containers and Goods Received subtabs
 * Read-only view of last 30 days data
 */
export default function DailyUpdatesPage() {
  return <CustomerDailyUpdates />;
}